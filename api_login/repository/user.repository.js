const conn = require("./../mysql.knex")

module.exports = {
    insert: async (data) => {
        // retunr await
    },
    delete: () => { },
    update: () => { },
    buscaTodos: () => { },
    loginUsuario : async (user_name, senha) =>{
        // SELECT COM WHERE
    }
}